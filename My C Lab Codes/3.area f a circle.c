#include<stdio.h>
int main()
{
    float R,area,pi=3.14159;
    printf(" Enter the radius of the circle: ");
    scanf("%f",&R);
    area=(pi*R*R);
    printf("\n The area of the circle is %f.\n",area);
    return 0;
}
